#include <cs50.h>
#include <stdio.h>

int main(void)
{

    int i ;
    int j ;
    int n1;
    do
    {
        n1 = get_int("Minimum: ");
    }
    while (n1 < 1);

    int n2;
    do
    {
        n2 = get_int("Maximum: ");
    }
     while (n1 >= n2);

    for ( i = n1; i <= n1; i++)
    {
        for ( )

        if (  == 0)

        {
            printf("%i\n", n);

        }
    }
}

